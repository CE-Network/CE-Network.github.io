source "https://rubygems.org"

# Pins Jekyll and every plugin to exactly the versions GitHub Pages
# builds with in production, so a local preview matches the live site.
gem "github-pages", group: :jekyll_plugins
